<Window x:Class="SuccessPlanner.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SuccessPlanner.App.ViewModels"
        xmlns:views="clr-namespace:SuccessPlanner.App.Views"
        Title="Success Planner MCP"
        Width="1100"
        Height="720"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F3EE"
        FontFamily="Segoe UI"
        Icon="Assets/success-planner-mcp.ico">
    <Window.Resources>
        <SolidColorBrush x:Key="InkBrush" Color="#171717" />
        <SolidColorBrush x:Key="MutedBrush" Color="#5F656C" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFCF7" />
        <SolidColorBrush x:Key="FocusRingBrush" Color="#2F6FED" />
        <DataTemplate DataType="{x:Type viewModels:HomeScreenViewModel}">
            <views:HomeScreenView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:CaptureViewModel}">
            <views:CaptureView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:InitialScreenViewModel}">
            <views:InitialScreenView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Margin" Value="10,0,0,0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D7DADF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#F1F5F9" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#B8C0CC" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#E8EDF3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="MinWidth" Value="46" />
            <Setter Property="Width" Value="46" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="ToolTipService.ShowDuration" Value="5000" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2F6FED" />

        <Grid Grid.Column="1" Margin="32,28,32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Content="&#xE72B;"
                        Command="{Binding GoBackCommand}"
                        Style="{StaticResource NavButtonStyle}"
                        ToolTip="Back"
                        IsEnabled="{Binding CanGoBack}" />
                <Button Content="&#xE80F;"
                        Command="{Binding GoHomeCommand}"
                        Style="{StaticResource NavButtonStyle}"
                        ToolTip="Home" />
                <Border Width="52"
                        Height="52"
                        CornerRadius="8"
                        Background="#FFFFFF"
                        BorderBrush="#E5E1D8"
                        BorderThickness="1"
                        Margin="0,0,16,0">
                    <Grid>
                        <TextBlock Text="&#xE8A5;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="23"
                                   Foreground="#2F6FED"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Success Planner MCP"
                               FontSize="32"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource InkBrush}" />
                    <TextBlock Text="Capture simply. Focus briefly. Build momentum."
                               FontSize="15"
                               Foreground="{StaticResource MutedBrush}"
                               Margin="0,3,0,0" />
                </StackPanel>
            </StackPanel>

            <Border Grid.Column="1"
                    Padding="14,8"
                    CornerRadius="8"
                    Background="#E6F4EA"
                    BorderBrush="#CDEAD5"
                    BorderThickness="1"
                    VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="#24984E" Margin="0,0,8,0" />
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="#1E6B3A"
                               FontWeight="SemiBold"
                               FontSize="13" />
                </StackPanel>
            </Border>
        </Grid>

        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentScreen}"
                        Margin="0,34,0,30" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding FooterText}"
                       Foreground="{StaticResource MutedBrush}"
                       VerticalAlignment="Center"
                       FontSize="13" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource ActionButtonStyle}" Command="{Binding OpenFindCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="Find" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource ActionButtonStyle}" Command="{Binding OpenSettingsCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="Settings" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
        </Grid>
    </Grid>
</Window>
